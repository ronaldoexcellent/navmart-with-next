import React from "react";

const Loading = () => {
    return (
        <main className="text-center mt-16">
            <h2 className="text-green-600 font-semibold text-2xl">Loading...</h2>
            <p>Hopefully not too long :)</p>
        </main>
    )
}

export default Loading;